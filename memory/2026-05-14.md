# 2026-05-14 记忆日志

## 核心事件
- 修复自动播放导航 bug + UX 增强（toast 提示、箭头禁用态）

## 重要决策
1. **`apPrev()` 索引修复**：之前 `_apIdx--` 导致 prev 只回到当前词（原地踏步）。修正为：
   - 计算当前显示索引 `cur = _apIdx - (_apPaused ? 0 : 1)`
   - `_apIdx = cur - 1` 才能回到前一个词
2. **禁用态 CSS 方案**：用 `.disabled` class + `pointer-events:none` 阻止点击，而非 `disabled` 属性（保持 onclick 可追溯）
3. **`apSyncNavDisabled()` 与 `gApSyncNavDisabled()` 统一模式**：两个自动播放系统共享同一套 nav 禁用逻辑

## 修改文件
- `inline.js`：apPrev/apNext 索引修正 + toast + apSyncNavDisabled + gApSyncNavDisabled
- `index.html`：CSS 禁用态样式 + cache buster v109→v110

## 待办
- [ ] 等待用户反馈 v110 自动播放体验
- [ ] 继续跟踪「测试 submit_to_agent 唤醒冻结 session」

## 团队动态
- 无其他 Agent 活动
